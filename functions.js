let person = {
  name: "Dennis',
  age: 30
};

//displaying results person function
console.log(person);

// 2) Dot Notation (most common)
person.name = 'Kwasi';
console.log(person.name);

// 3) Bracket Notation
person['name'] = 'Nyarko';
console.log(person.name);
