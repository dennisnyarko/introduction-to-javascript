// Performing a task
function greet() {
 console.log('Good Morning');   
}

greet();


//2nd example
// used a different function and variable name
function welcome(fname) {
    console.log('Good Morning ' + fname);   
   }
   
   welcome('Dennis');

   //treat codes as separate
   //comment out unwanted lines

   // Calculating a value
   function square(number) {
       return number * number;
   }

   let number = square(2);
   console.log(number); // you can do this straight without declaring a variable
